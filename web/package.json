{
  "name": "wiki-garden-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Local web UI for `garden tend`. Built with Vite + Svelte 5, consuming vendored @kenn-io/kit-ui. Build output is committed to skills/wiki-garden/wiki_garden/web-dist so the skill ships without a JS toolchain.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "7.1.2",
    "@tsconfig/svelte": "5.0.8",
    "svelte": "5.56.3",
    "svelte-check": "4.6.0",
    "typescript": "^5.9.0",
    "vite": "6.3.6"
  },
  "dependencies": {
    "@lucide/svelte": "1.21.0",
    "dompurify": "^3.2.0",
    "marked": "^18.0.0",
    "shiki": "^4.0.0"
  }
}
